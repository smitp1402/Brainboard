# Dependencies and build output
node_modules/
dist/

# Local npm pack output — the registry is the source of truth for releases
*.tgz

# Secrets — never commit. .env.example is the tracked template.
.env
.env.*
!.env.example
.npmrc

# Local notes
STUDY_GUIDE.md

# Logs and OS noise
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Personal notes — kept locally, not published
QA_LOG.md
brainboard-mcp/docs/TYPESCRIPT-NOTES.md

# Outreach drafts — personal, not published
contact/

# Demo/presenter materials — not published
PRESENTER_SCRIPT.md
ARCHITECTURE_EXPLAINED.md
ARCHITECTURE_DIAGRAMS.html
founder_demo.html
API_SPEC_VALIDATION_REPORT.md
DEMO_COMMANDS.md
DEMO_COMMANDS_CHECKLIST.md
BRAINBOARD_MCP_REPORT.md

# Exported pitch/report documents — the Markdown sources are the originals
*.pdf
*.docx

# Credentials kept locally by mistake. These are recovery codes, not config:
# they belong in a password manager, not in a git working tree.
npm_recovery_codes*.txt

# Claude Code tooling — local only
.claude/
.mcp.json
